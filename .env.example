# Server Configuration
PORT=8080
HOST=localhost

# Database Configuration
DB_DRIVER=sqlite
DB_NAME=notes.db

# Environment
ENV=development

# CORS Configuration (if needed later)
CORS_ALLOWED_ORIGINS=*